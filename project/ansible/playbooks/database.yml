---
# database.yml - Плейбук для настройки PostgreSQL
# Применяется только к серверам из группы [database] в инвентарном файле

- name: Configure PostgreSQL database server
  hosts: database
  become: yes
  gather_facts: yes
  
  roles:
    - postgresql